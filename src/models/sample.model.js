/**
 * MODELS — Database schemas and data shape definitions.
 *
 * Create one model file per entity:
 *   - user.model.js       → User schema (name, email, password, role)
 *   - token.model.js      → Refresh token or password reset token schema
 *   - post.model.js       → Blog post or content schema
 *   - file.model.js       → Uploaded file metadata schema
 *
 * Adapt to your ORM/ODM of choice:
 *   - Mongoose  (MongoDB)
 *   - Sequelize (PostgreSQL, MySQL, SQLite)
 *   - Prisma    (PostgreSQL, MySQL, MongoDB, SQLite)
 *   - Knex      (PostgreSQL, MySQL, SQLite)
 *
 * Naming convention: {entity}.model.js
 */
